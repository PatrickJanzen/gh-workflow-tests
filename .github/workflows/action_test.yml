name: 'LabelTest'
on:
  pull_request:
    types: [ opened, synchronize ]
    branches:
      - 'master'
  pull_request_target:
    types:
      - closed
    branches:
      - 'master'
jobs:
  test1:
    name: Test 1
    runs-on: ubuntu-latest
    steps:
      - id: isMaster
        run: echo ${GITHUB_HEAD_REF}